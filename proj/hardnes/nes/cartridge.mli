open! Core
open! Hardcaml
(*
   module I : sig
  type 'a t =
    { clock : 'a
    ; clear : 'a
    ; cpu : 'a Cpu_bus.I.t
    ; ppu_addr : 'a
    ; ppu_data_in : 'a
    ; ppu_read : 'a
    ; ppu_write : 'a
    }
  [@@deriving hardcaml]
end

module O : sig
  type 'a t =
    { cpu : 'a Cpu_bus.O.t
    ; ppu_data : 'a
    ; irq : 'a
    }
end *)
